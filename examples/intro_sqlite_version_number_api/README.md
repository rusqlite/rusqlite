# SQLite version statement example

This example returns SQLite library version number via the dedicated API.

## Run

```
$ cargo run --example intro_sqlite_version_number_api --features bundled
```
